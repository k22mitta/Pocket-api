# Copy to .env.local and fill in real values. Never commit .env.local.

# Base URL of the Go API. Defaults to http://localhost:8080 if unset — only
# needs to be set explicitly for a deployed backend.
NEXT_PUBLIC_API_URL=http://localhost:8080
